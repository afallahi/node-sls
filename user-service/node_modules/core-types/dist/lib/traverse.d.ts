import { NodePath, NodeType } from "./types";
export interface TraverseCallbackArgument {
    node: NodeType;
    rootNode: NodeType;
    path: NodePath;
    parentProperty?: string;
    parentNode?: NodeType;
    index?: string | number;
    required?: boolean;
}
export declare type TraverseCallback = (arg: TraverseCallbackArgument) => void;
export declare type SomeCallback = (arg: TraverseCallbackArgument) => boolean;
export declare function traverse(node: NodeType, cb: TraverseCallback): void;
export declare function some(node: NodeType, cb: SomeCallback): boolean;
