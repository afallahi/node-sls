import { RBTree } from 'bintrees';
export declare type Compare<T> = (a: T, b: T) => number;
export declare class ShortTree<T> extends RBTree<Array<T>> {
    private cmp;
    private arrayCmp;
    constructor(cmp: Compare<T>, arrayCmp?: Compare<T[]>);
    private arrayStartsWith;
    insert(path: Array<T>): boolean;
    private chopOff;
    values(): Array<Array<T>>;
}
