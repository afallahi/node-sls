import { LocationOptions, ParsedJson } from 'jsonpos';
export interface CodeOptions extends LocationOptions {
    linesAbove?: number;
    linesBelow?: number;
}
export declare function printCode(message: string | undefined, parsedJson: ParsedJson, { dataPath, markIdentifier, linesAbove, linesBelow, }: CodeOptions): string;
