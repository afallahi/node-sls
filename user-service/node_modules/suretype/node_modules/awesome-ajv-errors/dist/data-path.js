"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseDataPath = void 0;
function parseDataPath(value) {
    const path = [].concat(...value
        .split('.')
        .map(entry => entry
        .split(/\[|\]/g)
        .map((key, index) => ({
        key,
        type: index === 0 ? 'string' : 'number'
    }))))
        .filter(val => val.key !== '');
    const dotOnly = '.' + path.map(({ key }) => key).join('.');
    return { orig: value, dotOnly, path };
}
exports.parseDataPath = parseDataPath;
